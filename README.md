# TGPT (Ollama Version)

A simple TGPT alternative using Ollama (no API key, fully offline).

## Install

```bash
git clone https://github.com/yourname/tgpt-ollama
cd tgpt-ollama
chmod +x install.sh
./install.sh
